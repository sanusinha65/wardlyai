services:
  wardly:
    build:
      context: .
    image: wardlyai:local
    ports:
      - "8080:80"
    environment:
      VITE_GEMINI_API_KEY: ${VITE_GEMINI_API_KEY:-}
      VITE_GEMINI_MODEL: ${VITE_GEMINI_MODEL:-}
      VITE_SPEECH_LANG: ${VITE_SPEECH_LANG:-}
